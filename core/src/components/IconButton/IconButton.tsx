import BaseComponent from '../../workflow/BaseComponent'
import MuiIconButton from '@mui/material/IconButton'

export default class IconButton extends BaseComponent {
    name = 'IconButton'

    constructor(props) {
        super(props)
    }

    render() {
        return 22
        // return <MuiIconButton></MuiIconButton>
    }
}
